# Enable split keyboard support
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# Use UART for wired split (not BLE)
CONFIG_ZMK_SPLIT_BLE=Y

# Disable Bluetooth entirely for wired keyboard
CONFIG_BT=Y
CONFIG_ZMK_BLE=Y
